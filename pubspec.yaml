name: ndarray
description: N-Dimensional Arrays in Dart.
version: 0.0.1
homepage: dashflow-io.github.io/ndarray

environment:
  sdk: '>=3.0.0-19.0.dev <4.0.0'
  flutter: ">=2.11.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  ffi: ^2.0.1
  ffigen: ^7.2.3
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0